import { ResponseStatusDTO } from './response-status-dto.model';

describe('ResponseStatusDTO', () => {
  it('should create an instance', () => {
    expect(new ResponseStatusDTO()).toBeTruthy();
  });
});
